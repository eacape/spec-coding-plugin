# 下一轮开发 Prompt（参照 docs/dev-checklist.md + docs/dev-process.md）

你现在在仓库 `D:/eacape/spec-coding-plugin` 中继续开发，请严格按以下要求执行：

## 目标选择
- 先阅读：
  - `docs/dev-checklist.md`
  - `docs/dev-process.md`
- 按优先级选择“最高优先级且未完成”的开发项继续推进。
- 暂不开发：`Gemini / Ollama` 提供者扩充。
- 默认优先：`2.22 高级功能` 中与当前进度连续的未完成项  
  `同工作流历史版本对比（按时间/版本）` 与 `变更摘要导出` 的 MVP。

## 执行要求
- 先做最小可用闭环（MVP），避免大而全。
- 代码改动范围尽量收敛，只改与目标直接相关的文件。
- 所有用户可见文案必须走 `SpecCodingBundle.message(...)`，中英文 key 同步补齐。
- 补充必要单元测试，至少覆盖：
  - 历史版本对比核心计算逻辑
  - 过滤/排序/摘要统计逻辑
  - 边界场景（空历史、单版本、内容完全一致）
- 完成后跑定向测试 + i18n 一致性测试。

## 交付清单
- 代码实现（历史版本对比 + 摘要导出 + 必要 UI/配置接入）
- 测试与通过结果（命令 + 关键结果）
- 文档同步：
  - `docs/dev-checklist.md` 勾选本轮已完成项
  - `docs/dev-process.md` 追加一条新 Session（含“开发前任务/完成工作/当前状态/下一步计划”）

## 质量与安全
- 不引入硬编码密钥。
- 保持现有架构边界，不跨模块直接依赖实现类。
- 不做无关重构，不回退他人已有改动。
